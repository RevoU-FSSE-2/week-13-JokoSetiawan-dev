import React from "react";
import styles from "./home.module.css";

const HomePage: React.FC = () => {
  return (<div className={`container ${styles.homeContainer}`}>
    Home
    </div>)
};

export default HomePage;
